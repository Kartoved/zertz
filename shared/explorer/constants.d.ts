// Ambient types so TypeScript clients can import the plain-JS constants module
// without `allowJs`. Mirrors shared/explorer/constants.js.

export declare const MARBLE_SUPPLY: { white: number; gray: number; black: number };
export declare const WIN_CONDITIONS: { white: number; gray: number; black: number; allColors: number };
