# Postgres connection (local defaults)
PGHOST=localhost
PGPORT=5432
PGDATABASE=zertz
PGUSER=postgres
PGPASSWORD=postgres

# API port
PORT=5050

# JWT secret for authentication (change in production!)
JWT_SECRET=zertz-dev-secret-change-me-in-production
